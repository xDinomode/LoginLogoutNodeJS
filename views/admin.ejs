<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>hi</title>
    <link rel="stylesheet" href="/stylesheets/admincss.css">
</head>
<body>
        <header id = "topHeader">
             <h1>Admin page</h1>
        <% if(user != null) {%>
                <div>
                    <p><%= user.username %></p>
                </div>
        <% }%>
        </header>
        <a href = "/logout">Logout</a>

        <script src="/bower_components/jquery/dist/jquery.min.js"></script>
       <!--<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script>-->
        <script>
            $(function(){
                $.ajax({
                    type: "GET",
                    url: "/h",
                    success: function(data){
                        $.each(data, function(i, one){
                            console.log(one.username, one.password);
                        });
                    }
                });
            });
        </script>
</html>
